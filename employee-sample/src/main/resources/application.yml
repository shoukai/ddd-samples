# DataSource Config

#spring:
#  datasource:
#    driver-class-name: org.h2.Driver
#    schema: classpath:db/schema-h2.sql
#    data: classpath:db/data-h2.sql
#    url: jdbc:h2:mem:test
#    username: root
#    password: test

spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/local?serverTimezone=UTC&useSSL=false&allowPublicKeyRetrieval=true
    username: mysql
    password: mysql

# Logger Config
logging:
  level:
    com.baomidou.mybatisplus.samples.quickstart: debug